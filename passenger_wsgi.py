import sys, os

# INTERPRETER = "/home/yourusername/virtualenv/yourproject/3.X/bin/python"
# if sys.executable != INTERPRETER: os.execl(INTERPRETER, INTERPRETER, *sys.argv)

from app import app as application
